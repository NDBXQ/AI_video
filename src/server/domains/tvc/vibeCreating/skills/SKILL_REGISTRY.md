# Skills 注册表

本文件注册所有可用的 Claude Skills，提供技能索引和元数据。

## Skills 列表

| Skill ID | 名称 | 描述 | 版本 | 状态 |
|----------|------|------|------|------|
| `tvc-script` | TVC剧本创作 | 创作TVC广告剧本大纲 | 1.0.0 | ✅ |
| `tvc-storyboard` | TVC脚本创作 | 创作详细的分镜头脚本 | 1.0.0 | ✅ |
| `tvc-reference-images` | TVC参考图生成 | 批量生成参考图片 | 1.0.0 | ✅ |
| `tvc-video-generation` | TVC视频生成 | 批量生成视频片段 | 1.0.0 | ✅ |
| `tvc-background-music` | TVC背景音乐 | 推荐并合成背景音乐（通常合并在 tvc-video-generation 中使用） | 1.0.0 | 💤 |
| `tvc-orchestrator` | TVC流程编排 | 编排完整的TVC创作流程 | 1.0.0 | ✅ |

## 使用说明

### 加载方式
系统不会自动触发 Skill；模型需要通过工具 load_skill_instructions 主动加载并遵循对应的 SKILL.md 规范。
除 tvc-orchestrator/tvc-script/tvc-storyboard 的结构化产物外，其它技能默认只用自然语言与用户沟通；素材展示由前端监听+拉取资源完成。

### 手动触发
使用 `/skill-name` 格式手动触发特定 Skill，例如：
- `/tvc-script` - 手动触发剧本创作
- `/tvc-reference-images` - 手动触发参考图生成

### Skill 依赖关系

tvc-orchestrator（流程编排）
- tvc-script（剧本）
- tvc-reference-images（参考图）
- tvc-storyboard（分镜）
- tvc-first-frame（首帧）
- tvc-video-generation（视频与音乐）

## 工具对齐

本项目实际可调用的工具名称以服务端 tool schema 为准（例如：generate_images_batch、generate_videos_from_images_batch、recommend_background_music、assets_resolve）。

## 更新日志

### 2024-01-20
- 初始版本发布
- 注册 6 个 TVC 创作相关 Skills
